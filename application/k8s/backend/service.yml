apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  type: ClusterIP  # Changed from NodePort to ClusterIP (Internal Only)
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001